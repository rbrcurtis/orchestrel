# Server
PORT=6194
HOST=0.0.0.0

# OpenCode (AI agent runtime)
OPENCODE_PORT=4097

# Cloudflare Access (optional — only needed if exposing via Cloudflare Tunnel)
# CF_TEAM_DOMAIN=your-team    # <team>.cloudflareaccess.com
